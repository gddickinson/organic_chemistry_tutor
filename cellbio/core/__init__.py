"""Cell Bio Studio — pure-data catalogues + algorithms.

No Qt imports here.  Each catalogue is a frozen dataclass + module-
level constant + lookup helpers + ``to_dict`` serialiser, mirroring
the pattern in ``orgchem/core/``.
"""
